% OGC2026 Technical Report Template
% Based on the Springer LNCS document class

\documentclass[runningheads]{llncs}

\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{url}
\usepackage{xcolor}
\usepackage[most]{tcolorbox}
\usepackage{comment}
\usepackage{iftex}

\urlstyle{rm}

\newif\ifshowguidance
\showguidancefalse

\ifshowguidance
    \newtcolorbox{guidance}{
        enhanced, breakable, colback=blue!4, colframe=blue!55!black,
        coltext=blue!70!black, colbacktitle=blue!55!black, coltitle=white,
        boxrule=0.5pt, arc=1mm, left=2mm, right=2mm, top=1.5mm, bottom=1.5mm,
        before skip=5pt, after skip=8pt, title={Writing Guidelines},
        fonttitle=\small\bfseries, fontupper=\small
    }
\else
    \excludecomment{guidance}
\fi

\begin{document}

\title{OGC2026 Technical Report}
\maketitle

\begin{abstract}
This report presents a heuristic for a block scheduling problem that simultaneously involves temporal constraints and geometric constraints on multilayer polygons. For each block, the problem requires determining its bay, orientation, coordinates, entry time, and exit time; in particular, crane accessibility for entry and exit strongly couples placement and time. The core of the proposed method consists of abstract preoptimization, which relaxes the exact geometric constraints into time-indexed area-capacity constraints, and rolling-horizon optimization following the admission order obtained from the abstract solution. In each horizon, a feasible solution is constructed by greedy insertion that integrates a spatial sweep with forbidden entry-time intervals, and is improved by parallel simulated annealing with several neighborhoods, including large reconstruction that removes and reinserts multiple blocks. Because it is difficult to move from a dense packing to a structurally different arrangement using local changes alone, the method emphasizes obtaining a good admission order in advance and optimizing every block at least once while it is near the end of the partial schedule.
\end{abstract}

\section{Introduction}

\subsection{Challenge Problem \& Main Difficulties}

For every block $i$, we determine its assigned bay $b_i$, orientation $o_i$, coordinates $(x_i,y_i)$, entry time $e_i$, and exit time $q_i$. The temporal constraints are
\begin{equation}
e_i\ge R_i,\qquad q_i-e_i\ge P_i.
\end{equation}
Blocks occupying the same bay at the same time must not overlap at layers of the same height. Moreover, during an ENTRY or EXIT operation, vertically moving the target block must not collide with layers of other blocks at the same or a greater height than each target layer. Consequently, even a statically non-overlapping placement may be infeasible for crane operations.

The objective is the weighted sum of total tardiness $Z_1$, workload imbalance among bays $Z_2$, and bay-preference penalty $Z_3$:
\begin{equation}
F=w_1Z_1+w_2Z_2+w_3Z_3.
\end{equation}
In particular,
\begin{equation}
Z_1=\sum_i\max(0,q_i-D_i),\qquad
Z_3=\sum_i(S_i^{\max}-S_{i,b_i}),
\end{equation}
so $Z_1$ and $Z_3$ decompose into block-wise contributions. Whether these values can be realized, however, depends on the placements and residence intervals of other blocks.

The primary difficulty is the rigidity of the search space. Once a bay becomes densely packed, the space created by moving a single block is often insufficient to improve another block; changing the packing structure requires coordinated changes to several blocks. Furthermore, moving an early block changes its collision relationships and crane ordering with many later blocks. Search performance therefore depends strongly on the initial solution, especially the order in which blocks are admitted to the geometric schedule.

\subsection{Contributions}

The main contributions of this report are as follows.
\begin{itemize}
    \item We introduce abstract preoptimization that relaxes multilayer polygon constraints into time-indexed area-capacity constraints and obtains an admission order from the bay assignment and entry time of every block.
    \item We construct a rolling-horizon framework that expands partial problems according to the abstract admission order, allowing every block to be optimized once while it is at the flexible end of the partial schedule.
    \item We develop greedy insertion that jointly searches space and time by combining an $x$-direction sweep for multilayer collisions, based on Minkowski differences, with forbidden entry-time intervals induced by crane constraints.
    \item We combine small neighborhoods with large reconstruction, which structurally selects and reinserts multiple related blocks, within parallel simulated annealing.
\end{itemize}

\section{Algorithm Description}

\subsection{Overall Algorithm Framework}

\subsubsection{Key findings and design rationale.}
The first key observation concerns a conflict between tardiness and
packability.  Every unit of block tardiness has the same weight in $Z_1$,
regardless of the size of the block.  Given one large block and several small
blocks with similar ENTRY/EXIT conditions, processing the small blocks first
can therefore reduce total tardiness.  Geometrically, however, a large block
is easier to insert while ample free space remains, and inserting it first may
produce a better packing.  Consequently, an order favorable for $Z_1$ does
not necessarily coincide with an order favorable for geometric feasibility.
A simple release-time or due-date order cannot capture this trade-off together
with processing times, bay capacities, bay preferences, and competition among
blocks.

The second observation is that the geometric constraints cause strong
lock-in.  Once a schedule has been packed densely, moving to a substantially
different packing by changing one block at a time is difficult.  Moreover,
moving a block changes its temporal and crane-obstruction relationships with
blocks scheduled after it.  A block near the beginning of a completed
schedule is therefore constrained by many subsequent blocks, whereas a block
near the end has few or no subsequent blocks and is much easier to relocate.
These properties make both the initial admission order and the stage at which
each block is optimized important.

We address the admission-order problem by abstract preoptimization.  Exact
polygon constraints are relaxed into time-indexed area-capacity constraints,
and the relaxed problem is solved over all blocks to obtain a bay $\hat b_i$
and a desirable entry time $\hat e_i$ for every block.  Sorting by $\hat e_i$
provides an admission order that reflects global competition for capacity and
the resulting tardiness, rather than relying on a local priority such as
release time or due date.  The purpose of this stage is not to determine final
coordinates, but to predict an effective order in which the exact geometric
schedule should be constructed.

We then convert this global order into an exact schedule by rolling-horizon
optimization.  Each horizon admits only a limited number of new blocks and
optimizes a partial problem.  This decomposition is well aligned with the
original objective because its dominant terms in many instances are
block-wise additive:
\begin{equation}
Z_1=\sum_i T_i, \qquad
Z_3=\sum_i \left(S_i^{\max}-S_{i,b_i}\right).
\end{equation}
Improving these terms for admitted blocks therefore directly contributes to
improving the corresponding terms of the full problem.  In addition, every
block is once newly admitted near the tail of a partial schedule, where it can
be moved with relatively few dependencies on subsequent blocks.  Limiting the
number of admitted blocks also reduces the cost of neighborhood evaluation
and permits more search iterations than repeatedly optimizing the complete
problem.  Earlier blocks are not frozen: all admitted blocks remain movable,
so decisions can still be corrected when later blocks are introduced.

Finally, rolling-horizon optimization alone does not eliminate geometric
lock-in within each partial problem.  In a dense schedule, single-block
neighborhoods such as Shift and Move often cannot cross infeasible
intermediate configurations.  Large reconstruction therefore removes several
spatially, temporally, or objectively related blocks and reinserts them in a
new order, allowing the packing structure to change in one neighborhood move.
The three main ideas thus have complementary roles: abstract preoptimization
determines a globally informed admission order, rolling-horizon optimization
gives every block a relatively flexible stage for exact optimization, and
large reconstruction enables structural changes after blocks have been
packed.

\subsubsection{Solution procedure.}
The solver first precomputes geometric information and candidate relations,
and then solves the area-capacity relaxation to obtain $(\hat b_i,\hat e_i)$
for every block. Blocks are admitted in lexicographic order of
$(\hat e_i,D_i,i)$, without splitting equal $\hat e_i$ at a horizon boundary.
Each horizon greedily inserts its new blocks and improves all admitted blocks;
the final horizon uses the complete objective containing $Z_1$, $Z_2$, and
$Z_3$. Algorithm~\ref{alg:overall} summarizes this procedure, focusing on how
the abstract schedule is converted into an exact geometric schedule. The
abstract entry times determine the admission order and provide a reference
solution for insertion, but neither the bay nor the entry time is fixed in the
geometric optimization. In particular, $\hat e_i$ is not imposed as a lower
bound on the exact entry time.

\begin{algorithm}[t]
\caption{Overall solution procedure}
\label{alg:overall}
\begin{algorithmic}[1]
\Require problem instance, horizon size $H$, global deadline $\tau$
\Ensure feasible geometric schedule $S$
\State $\{(\hat b_i,\hat e_i)\}_i \gets \Call{AbstractPreoptimization}{}$
\State $\pi \gets$ blocks sorted lexicographically by $(\hat e_i,D_i,i)$
\State $\mathcal{H} \gets \Call{BuildHorizons}{\pi,H}$
       \Comment{do not split equal $\hat e_i$}
\State $S\gets\emptyset$
\For{each horizon $h\in\mathcal{H}$}
    \For{each newly admitted block $i$ in $h$}
        \State $S\gets\Call{GreedyInsert}{S,i,\hat e_i}$
    \EndFor
    \If{$h$ is the final horizon}
        \State $w'_2\gets w_2$
    \Else
        \State $w'_2\gets 0$
    \EndIf
    \State $\tau_h\gets\Call{AllocateRemainingTime}{h,\mathcal{H},\tau}$
    \State $S\gets\Call{ParallelSA}{S,w_1,w'_2,w_3,\tau_h}$
       \Comment{optimize all admitted blocks}
\EndFor
\State \Return $S$
\end{algorithmic}
\end{algorithm}

\subsection{Key Ideas that Succeeded}

\subsubsection{Abstract preoptimization.}
For orientation $o$ of block $i$, let $A^{\mathrm{union}}_{io}$ be the area of the union of all layers, $A^{\mathrm{bbox}}_{io}$ be the area of its bounding box, and $L_{io}$ be the perimeter of the union. If orientation $o$ fits in bay $j$, its approximate occupancy is
\begin{equation}
a_{ijo}=A^{\mathrm{union}}_{io}
+\alpha\left(A^{\mathrm{bbox}}_{io}-A^{\mathrm{union}}_{io}\right)
+\beta L_{io}.
\end{equation}
We define $a_{ij}$ as the minimum value over orientations that fit in bay $j$. The first term represents the actual occupied area, the second approximates unusable space around concave or elongated shapes, and the third compensates for packing difficulty caused by a complex boundary.

Let $C_j$ be the effective capacity of bay $j$. The abstract schedule must satisfy
\begin{equation}
\sum_{i:\,\hat e_i\le t<\hat e_i+P_i,\,\hat b_i=j}a_{ij}\le C_j
\end{equation}
at every time. An abstract state stores only $(\hat b_i,\hat e_i)$ for each block and does not store its exact orientation or coordinates. Its objective consists of $Z_1$, $Z_2$, and $Z_3$ evaluated on the abstract schedule, plus a congestion penalty. The implementation sums products of the occupancy ratios of simultaneously resident block pairs over time. This discourages highly congested abstract schedules that satisfy the area capacity but are difficult to convert into exact placements.

Initial states are built greedily from multiple randomized block orders. Each order combines workload, the product of area and processing time, the spread of bay preferences, the limit time defined as due date minus processing time, slack, and a random component. For each block, the earliest capacity-feasible time is found in every eligible bay, and the candidate minimizing the incremental $Z_1$, $Z_2$, $Z_3$, and congestion is selected. The state is then improved by simulated annealing using relocate, swap, and multi-block reconstruction neighborhoods.

\subsubsection{Rolling-horizon geometric optimization.}
Blocks are sorted by $(\hat e_i,D_i,i)$, and horizons are formed with a prescribed block count. Blocks with the same $\hat e_i$ at a horizon boundary are included in the same horizon. At each horizon, newly admitted blocks are first inserted using the abstract schedule as a reference. Simulated annealing is then applied not only to the new blocks but to all blocks admitted so far. Unlike a freezing-type rolling horizon, earlier decisions can therefore be revised in response to later admissions.
This follows the general rolling-horizon principle of solving a sequence of restricted scheduling problems\cite{ouyang2021}, while retaining all previous blocks as movable variables in our geometric search.

Because $Z_2$ is not meaningful until the bay assignments of all blocks are present, its weight is set to zero in intermediate horizons. The official $w_2$ is enabled only in the final horizon. Search time is allocated with weights proportional to a power of the number of admitted blocks, reserving more time for the larger late-stage problems.

Specifically, let $N_h$ denote the cumulative number of admitted blocks in
horizon $h$, and let $\gamma$ be a parameter.  Ignoring time already consumed
by construction and other overhead, the nominal allocation is
\begin{equation}
\tau_h=\tau_{\mathrm{total}}
\frac{N_h^{\gamma}}{\sum_q N_q^{\gamma}}.
\end{equation}
In the implementation, this ratio is recomputed from the remaining horizons
and the remaining time at the beginning of every horizon, so earlier
overruns do not invalidate the global deadline.  We use $\gamma>1$ because
later horizons contain more interacting blocks, have more expensive
neighborhood evaluations, and are closer to the final complete objective.
At the same time, early horizons still receive enough time to establish a
packing structure before additional blocks reduce the available space.

\subsubsection{Greedy geometric insertion and collision detection.}
Greedy insertion enumerates bays, orientations, and integer $y$ coordinates, while sweeping in the $x$ direction through collision events. A candidate $s$ is compared primarily by
\begin{equation}
\Delta F(s)=w_1T_i(s)+w_2\Delta Z_2(s)
+w_3(S_i^{\max}-S_{i,b_i(s)}).
\end{equation}
Among candidates tied with the best objective value, an earlier entry time and placement near a bay corner are preferred. The default anchor is the bottom-left corner, but one of the other three corners is selected with a small probability to generate different packing structures.

Algorithm~\ref{alg:greedy-insert} outlines the insertion procedure.  Rather
than testing every $(x,e_i)$, the sweep updates collision states at
$x$-interval endpoints and converts them into forbidden entry-time intervals.

\begin{algorithm}[t]
\caption{Greedy geometric insertion of block $i$}
\label{alg:greedy-insert}
\begin{algorithmic}[1]
\Require partial schedule $S$, block $i$, reference entry time $\hat e_i$
\State $s^*\gets\mathrm{none}$ and $F^*\gets+\infty$
\For{each eligible $(j,o,y)$ whose lower bound does not exceed $F^*$}
    \State $E\gets\Call{BuildXEvents}{S,i,j,o,y}$
    \For{each candidate $x$ in the event sweep over $E$}
        \State update directional collision states at $x$
        \State $\mathcal{I}\gets$ corresponding forbidden entry-time intervals
        \State $e\gets\Call{FirstFeasibleTime}{\mathcal{I},R_i}$
        \If{$e$ exists and $s=(j,o,x,y,e,e+P_i)$ is better than $s^*$}
            \State $s^*\gets s$ and $F^*\gets\Delta F(s)$
        \EndIf
    \EndFor
\EndFor
\State \Return $s^*$
\end{algorithmic}
\end{algorithm}

For collision detection, each nonconvex layer is decomposed into convex polygons by ear clipping. For two convex polygons $A$ and $B$, a collision occurs when their relative displacement $(\Delta x,\Delta y)$ belongs to the Minkowski difference\cite{ghosh1993}
\begin{equation}
A\oplus(-B)=\{a-b\mid a\in A,\ b\in B\}.
\end{equation}
This region is horizontally sliced at every integer $\Delta y$ and converted into intervals of forbidden integer $\Delta x$. A small margin prevents missed collisions due to numerical errors, and overlapping or adjacent intervals in the same row are merged. For crane constraints, forbidden regions are united for every moving layer $k$ and fixed layer $l\ge k$. Because crane obstruction is directional, the two directions of every orientation pair are stored separately. The collision grid is lazily constructed on first use and then cached.

\paragraph{Joint spatial-temporal sweep.}
The distinguishing feature of the insertion procedure is that spatial and temporal searches are not separated. For every existing block, the algorithm separately tracks whether the new block is obstructed by the existing block and whether the existing block is obstructed by the new block. Endpoints of forbidden $x$ intervals are treated as events, and these two directional states are updated during the $x$ sweep. From the current state and the residence interval of the existing block, the forbidden entry times of the new block can be represented by at most two intervals. All active forbidden intervals at the current coordinates are merged to find the earliest feasible entry time in the search range. Hence, the full schedule need not be revalidated at every coordinate and every day, and the running time does not directly depend on the time horizon.

Let $n_j$ be the number of blocks already placed in bay $j$, let $W_j$ and $H_j$ be its width and height, and let $O_i$ be the number of orientations of the inserted block. Treating the number of collision intervals per block pair as constant, event generation and counting sort for one $(o,y)$ require $O(n_j+W_j)$ time. A conservative bound including scans of forbidden time intervals at candidate positions is $O(W_j+n_j^2)$. The approximate worst-case complexity of inserting one block is therefore
\begin{equation}
O\left(\sum_j O_iH_j(W_j+n_j^2)\right).
\end{equation}

\subsubsection{Simulated annealing and large reconstruction.}
The local search is based on simulated annealing\cite{kirkpatrick1983} and uses five neighborhood types: Large Reconstruction, Shift, Move, Rotate, and Swap. Shift scans nearby $y$ coordinates while retaining the bay and orientation. Rotate changes to a geometrically similar orientation precomputed in advance. Move primarily selects a relatively small block and redetermines its bay, orientation, position, and time by greedy insertion. Swap exchanges the locations of two blocks with similar area and shape and reinserts both.

Large reconstruction is a destroy-and-repair large neighborhood.
It selects multiple seeds using three methods: badness, fluidity, and random
selection. Badness evaluates tardiness, preference penalty, and membership in
the bay with the heaviest workload. Fluidity favors blocks with large slack
and a small spread of bay preferences, which are easier to move. Blocks in the
same bay as each seed are ordered by
\begin{equation}
d(i,s)=\lambda_x(x_i-x_s)^2+\lambda_y(y_i-y_s)^2
+\lambda_t(e_i-e_s)^2,
\end{equation}
and nearby blocks are removed together. Multiple seeds make it possible to change several local structures in one move. The reinsertion order is determined from a weighted combination of workload, volume, preference spread, limit time, slack, current penalty, and randomness. These weights are randomized for every attempt, allowing the search to revisit the trade-off between tardiness and packability without being restricted to a fixed size or due-date order. Large reconstruction in abstract preoptimization is simpler because the abstract state has no coordinates.

Simulated annealing is executed by multiple workers in parallel. Each worker updates a shared best state and periodically imports a sufficiently better shared solution. During stagnation, the worker returns to its best state and reheats by temporarily increasing its temperature. Distinct temperature ranges are used when positive tardiness remains and after tardiness reaches zero: the former handles larger changes in $Z_1$, while the latter focuses on finer improvements, primarily to $Z_3$. A tabu set of schedule hashes suppresses short-term revisits to identical states.

\subsection{Key Ideas that Failed}

Beam-search reconstruction attempted to reuse block-insertion results relative to the same base schedule across multiple search states, thereby exploring reinsertion orders at lower computational cost. However, exploring diverse placement states proved more important than reusing insertion results, and the method was discarded.

Multi-stage optimization, which optimized tardiness, preference, and workload imbalance in separate stages, did not show a significant advantage over the standard search. We also tested adaptive annealing that derived its temperature from observed objective differences and a blocker-aware neighborhood that explicitly removed blocks obstructing crane operations. Both ideas appeared promising, but obtaining parameter settings that worked consistently across instances was difficult.

\subsection{Further Improvement Plan}

For abstract preoptimization, we plan to improve approximate occupancy and congestion so that the gap between abstract schedules and exact placements is reduced. For rolling-horizon optimization, horizon size and search-time allocation could be adapted to each instance. We also plan to include the ordering of ENTRY/EXIT operations at the same time and temporal-order changes produced by modifying exit times. For large reconstruction, blocker dependencies could be used more directly in selecting blocks for removal.

For simulated annealing, in addition to revisiting adaptive temperature control based on objective differences and acceptance ratios, we plan to introduce a more systematic parallel scheme. Workers would specialize in different temperature ranges or neighborhood compositions and periodically exchange states, explicitly separating structural changes by high-temperature workers from local improvement by low-temperature workers.

\section{Implementation Details}
\label{sec:implementation}

The solver is implemented in Rust. Convex decompositions, bounding boxes, feasible placement ranges in bays, orientation neighbors, and swap candidates are precomputed. Collision grids for block and orientation pairs are built on demand and shared throughout the subsequent search. The placement sweep processes only endpoints of forbidden $x$ intervals and uses counting sort and bitsets to maintain active time intervals.

The time limit is divided between abstract preoptimization and rolling horizon optimization. In the latter phase, the deadline of each horizon is computed from the sizes of the remaining horizons. The current shared best solution is emitted periodically during the search. EXIT operations are added to the output before ENTRY operations, ensuring that every EXIT precedes every ENTRY at the same time.

% A Python wrapper surrounds the Rust solver. It deduplicates emitted candidates, checks them in objective-value order, and independently validates assignments, temporal constraints, bay containment, multilayer collisions, and crane operations using Shapely. If the best candidate is infeasible, the wrapper falls back to the next previously emitted feasible candidate. Time for validation and returning the result is reserved in advance from the solver time limit.

\section{Computational Results}

All methods were evaluated once on the 40 instances in the final training
set. Experiments were conducted on an Apple M1 Pro with eight CPU cores and
32 GB of memory. Each run used four worker threads and a
time limit of 300 seconds. ``w/o preoptimization'' replaces abstract preoptimization with a
simple due-date admission order, while ``w/o rolling horizon'' admits all
blocks in a single horizon.  All runs produced feasible solutions.
Table~\ref{tab:aggregate-results} reports the objective components aggregated
over all instances, and Table~\ref{tab:case-results} reports the total
objective for each instance together with its numbers of bays and blocks.

\begin{table}[H]
  \centering
  \caption{Aggregated objective values on the training dataset. Lower is better.}
  \label{tab:aggregate-results}
  \begin{tabular}{lrrr}
    \hline
    Metric & Solution & w/o preoptimization & w/o rolling horizon \\
    \hline
    $Z_1$ & \textbf{412,209,673} & 455,157,137 & 452,373,802 \\
    $Z_2$ & 534,593 & 520,229 & \textbf{428,491} \\
    $Z_3$ & \textbf{35,526,753} & 38,580,980 & 43,548,790 \\
    Total & \textbf{448,271,019} & 494,258,346 & 496,351,083 \\
    \hline
  \end{tabular}
\end{table}

\begin{table}[t]
  \centering
  \caption{Case-wise total objective values. The best value in each row is bold.}
  \label{tab:case-results}
  \scriptsize
  \setlength{\tabcolsep}{4pt}
  \renewcommand{\arraystretch}{0.88}
  \begin{tabular}{rrrrrr}
    \hline
    Case & Bays & Blocks & Solution & w/o preopt. & w/o RH \\
    \hline
     1 & 3 & 150 & \textbf{200,600} & 254,893 & 294,493 \\
     2 & 3 & 250 & \textbf{51,514,630} & 56,503,516 & 56,822,966 \\
     3 & 4 & 200 & 3,861,664 & \textbf{3,727,803} & 3,827,482 \\
     4 & 4 & 250 & 1,495,165 & \textbf{1,284,783} & 1,842,738 \\
     5 & 2 & 150 & \textbf{6,128,553} & 6,379,911 & 6,538,337 \\
     6 & 4 & 250 & 4,453,575 & \textbf{4,395,833} & 4,595,350 \\
     7 & 3 & 150 & 344,892 & 294,701 & \textbf{286,315} \\
     8 & 4 & 200 & \textbf{57,275} & 60,110 & 64,021 \\
     9 & 3 & 200 & 4,787,490 & \textbf{4,515,461} & 5,387,753 \\
    10 & 2 & 150 & \textbf{2,514,378} & 3,196,467 & 3,362,278 \\
    11 & 4 & 250 & 6,158,320 & \textbf{6,153,697} & 6,731,431 \\
    12 & 5 & 300 & \textbf{108,905} & 109,140 & 109,631 \\
    13 & 2 & 300 & \textbf{65,583,089} & 71,348,407 & 69,348,586 \\
    14 & 4 & 250 & \textbf{509,993} & 580,705 & 590,104 \\
    15 & 3 & 250 & \textbf{9,117,871} & 10,285,613 & 10,778,146 \\
    16 & 5 & 300 & \textbf{844,749} & 1,242,098 & 2,748,614 \\
    17 & 3 & 250 & \textbf{4,166,279} & 4,582,154 & 4,792,621 \\
    18 & 3 & 200 & \textbf{7,760,385} & 8,633,235 & 8,996,725 \\
    19 & 3 & 200 & \textbf{14,253,229} & 15,638,146 & 16,154,618 \\
    20 & 3 & 250 & \textbf{7,226,052} & 8,020,782 & 9,047,633 \\
    21 & 2 & 150 & \textbf{601,313} & 629,425 & 689,846 \\
    22 & 3 & 150 & \textbf{1,698} & 1,964 & 1,964 \\
    23 & 5 & 300 & \textbf{10,860,631} & 11,985,764 & 12,359,963 \\
    24 & 3 & 150 & \textbf{1,401,057} & 1,702,012 & 2,014,100 \\
    25 & 2 & 300 & \textbf{65,341,045} & 74,668,806 & 72,491,167 \\
    26 & 2 & 300 & \textbf{22,713,804} & 26,057,775 & 24,568,661 \\
    27 & 4 & 200 & \textbf{113,267} & 137,490 & 181,368 \\
    28 & 4 & 250 & \textbf{12,835,222} & 13,600,998 & 14,918,839 \\
    29 & 4 & 200 & \textbf{172,024} & 182,248 & 205,686 \\
    30 & 5 & 300 & 1,290,398 & \textbf{1,261,997} & 1,707,717 \\
    31 & 3 & 200 & \textbf{2,514,690} & 3,028,944 & 3,303,448 \\
    32 & 4 & 200 & \textbf{545,663} & 603,104 & 603,576 \\
    33 & 2 & 150 & 452,702 & \textbf{372,622} & 987,223 \\
    34 & 5 & 300 & 128,823 & \textbf{127,209} & 148,527 \\
    35 & 4 & 250 & \textbf{81,715} & 83,496 & 140,982 \\
    36 & 2 & 300 & \textbf{69,984,887} & 78,781,616 & 75,387,709 \\
    37 & 2 & 150 & \textbf{14,170,930} & 15,999,477 & 15,299,472 \\
    38 & 4 & 200 & \textbf{6,466,785} & 7,300,523 & 6,973,239 \\
    39 & 2 & 150 & \textbf{41,480,168} & 43,320,130 & 44,791,617 \\
    40 & 2 & 300 & \textbf{6,027,103} & 7,205,291 & 7,256,137 \\
    \hline
  \end{tabular}
\end{table}

As shown in Table~\ref{tab:aggregate-results}, removing preoptimization
increased the total objective by 10.26\%, including
increases of 10.42\% in $Z_1$ and 8.60\% in $Z_3$. This result is consistent
with the intended role of the relaxed area-capacity model: determining an
admission order that accounts for competition among blocks more effectively
than a simple due-date order.

Table~\ref{tab:aggregate-results} also shows that removing the rolling horizon
increased the total objective by 10.73\%, with
increases of 9.74\% in $Z_1$ and 22.58\% in $Z_3$.  This is consistent with
the intended effects of the rolling horizon: solving smaller subproblems
allows more iterations, and every block is temporarily placed near the tail
of a partial schedule, where it can be moved without disturbing many
subsequent blocks.  The particularly large change in $Z_3$ is also consistent
with the interpretation that this additional flexibility helps select better
bay assignments.

In Table~\ref{tab:aggregate-results}, both ablations obtained smaller $Z_2$
values, but these gains were outweighed
by their deterioration in the dominant $Z_1$ and $Z_3$ terms.  The complete
method achieved the best total objective in 31 of the 40 rows of
Table~\ref{tab:case-results}, compared with eight instances for w/o
preoptimization and one for w/o rolling horizon.
Thus, abstract preoptimization and rolling-horizon optimization play
complementary roles: the former provides an effective global insertion order,
while the latter converts that order into high-quality geometric placements.

\section{Team Members' Contributions}

This section is not applicable because the team consists of a single member.

\section{AI Use Declaration}

In this project, primarily Codex with GPT 5.6 Sol High was used to assist with implementing algorithms conceived by the author.
For this technical report, it was used to organize the existing implementation and prepare the draft.
The algorithmic ideas and design decisions were made by the author, who also reviewed, validated, and revised the generated code and text.

\begin{thebibliography}{8}

\bibitem{kirkpatrick1983}
S. Kirkpatrick, C. D. Gelatt, Jr., and M. P. Vecchi,
``Optimization by Simulated Annealing,''
\emph{Science}, vol. 220, no. 4598, pp. 671--680, 1983.

\bibitem{ghosh1993}
P. K. Ghosh,
``A Unified Computational Framework for Minkowski Operations,''
\emph{Computers \& Graphics}, vol. 17, no. 4, pp. 357--378, 1993.

\bibitem{ouyang2021}
W. Ouyang, G. Dalle Ave, I. Harjunkoski, and L. Imsland,
``A Rolling Horizon Approach for Scheduling of Multiproduct Batch Production
and Maintenance Using Generalized Disjunctive Programming Models,''
\emph{Computers \& Chemical Engineering}, vol. 148, article 107268, 2021.

\end{thebibliography}

\end{document}
