[package]
name = "ogc2026"
version = "0.1.0"
edition = "2024"

[features]
local = []
anneal-visualizer = []
profile-reconstruct = []

[dependencies]
geo = "0.30"
rayon = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
rand_pcg = "=0.9.0"

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"
