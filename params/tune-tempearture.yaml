name: optimize-temperature2
version: "070"
base_params: params/default.yaml
suite: suites/tardiness-zero.json
timelimit: 60
jobs: 1

steps:
  - name: zero-tardiness-temperature2
    mode: combinations
    parameters:
      # w3基準: 全体に低温
      - annealing.optimize.worker_temperature_scale: 0.0

      # w3基準: 終盤の収束を強める
      # - annealing.optimize.zero_tardiness:
      #     temperature_w3_scale: [10.0, 0.2]
      #     exchange_threshold: {w1_scale: 0.5, w3_scale: 50.0}
      #   annealing.optimize.worker_temperature_scale: 0.0

      # # w3基準: 終盤まで探索性を残す
      # - annealing.optimize.zero_tardiness:
      #     temperature_w3_scale: [10.0, 3.0]
      #     exchange_threshold: {w1_scale: 0.5, w3_scale: 50.0}
      #   annealing.optimize.worker_temperature_scale: 0.0

      # # w3基準: 序盤を高温、終盤を低温
      # - annealing.optimize.zero_tardiness:
      #     temperature_w3_scale: [20.0, 0.5]
      #     exchange_threshold: {w1_scale: 0.5, w3_scale: 50.0}
      #   annealing.optimize.worker_temperature_scale: 0.0

      # # w3基準: 全体に高温
      # - annealing.optimize.zero_tardiness:
      #     temperature_w3_scale: [20.0, 2.0]
      #     exchange_threshold: {w1_scale: 0.5, w3_scale: 50.0}
      #   annealing.optimize.worker_temperature_scale: 0.0

      # # 初期スコア基準: 低め
      # - annealing.optimize.zero_tardiness:
      #     temperature_initial_score_per_block_scale: [10.0, 1.0]
      #     exchange_threshold: {w1_scale: 0.5, w3_scale: 50.0}
      #   annealing.optimize.worker_temperature_scale: 0.0

      # # 初期スコア基準: 高め
      # - annealing.optimize.zero_tardiness:
      #     temperature_initial_score_per_block_scale: [30.0, 3.0]
      #     exchange_threshold: {w1_scale: 0.5, w3_scale: 50.0}
      #   annealing.optimize.worker_temperature_scale: 0.0

      # # w3と初期スコア基準の小さい方を使う
      # - annealing.optimize.zero_tardiness:
      #     temperature_w3_scale: [20.0, 2.0]
      #     temperature_initial_score_per_block_scale: [30.0, 3.0]
      #     exchange_threshold: {w1_scale: 0.5, w3_scale: 50.0}
      #   annealing.optimize.worker_temperature_scale: 0.0

      # # workerごとに1～4倍の温度差をつける
      # - annealing.optimize.zero_tardiness:
      #     temperature_w3_scale: [10.0, 1.0]
      #     exchange_threshold: {w1_scale: 0.5, w3_scale: 50.0}
      #   annealing.optimize.worker_temperature_scale: 3.0

      # # 低温を基準にworkerごとに1～10倍の温度差をつける
      # - annealing.optimize.zero_tardiness:
      #     temperature_w3_scale: [5.0, 0.5]
      #     exchange_threshold: {w1_scale: 0.5, w3_scale: 50.0}
      #   annealing.optimize.worker_temperature_scale: 9.0
