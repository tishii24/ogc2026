precompute:
- 怪しいものはpythonに返してshapelyで厳密に計算する
- 高速化

solver:
- 初期解を軽くする
- 大域的最適化
- 安全で高速なものを提出する
- 時間を伸ばして伸び幅を見る
- 有望な近傍を前計算
  - ブロックごとに似ている(orientation,dx,dy)を前計算する
  - 似ているブロック間での(orientation,dx,dy)を前計算する
    - bboxだけで良いかも
  - 1:kで似ているブロックの位置を計算する
  - ブロックの有望な隣接位置を計算する
    - 辺の角度を求める
- t-intervalをmergeして、候補tがなくなったら打ち切る
- もっと再挿入近傍を軽くする
- 今のコードで、問題の入力によって、例外を発生しうるところを全て列挙してください
- 評価
  - bboxの右上
- 初期解
  - coreごとに異なる初期解を作る
  - ブロックの挿入順序
  　 - 大きいブロック
    - due-dateが近いもの
- 近傍
  - ブロックの位置を入れ替える
    - k個のブロックを削除する
      - TODO: 大きさを考慮する
    - 削除したブロックの周辺を候補として挿入し直す
    - bbox内で(x,y)が小さい順に試す
  - k個のブロックの位置を連動して動かす
    - dx=(-5..0),dy=(-5..0)を試す
    - TODO: 動かして、ぶつかったブロックを一緒に動かす
  - 1個のブロックのorientationを変更する
  - 1つの小さいブロックを別の位置に挿入する
    - 小さいブロックを選ぶ
- orientationを色々試す
- obj2,obj3の緩和解を求めて、それに合わせて詰めに行く
- 縦長のbayでは上から詰める
- 強い最適化
  - insertを評価して、ベストなinsertを探す
  - packing-scoreの計算（bboxの重なりなど）
  - 置ける面積を具体的に計算
  - 重なっている面積が少なくなる方に動かす
  - 操作順を考慮する
- 焼きなまし過程の可視化
- 並列annealing

stats:
- グループ化して平均を表示
- ベストを一番下に表示

other:
- py側に複数返して、feasibilityを確認する
- 難しいケースをaugmentationして評価
- 時間を延ばして評価
- 順位のつき方を確認
